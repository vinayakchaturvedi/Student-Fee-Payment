var bills = [
        {
            'Description': 'Registration Fee', 'Bill Date': '10-Dec-2020',
            'Deadline': '15-Dec-2020', 'Total Amount': '1000', 'Paid Amount': '0'
            , 'Remaining Amount': '1000'
        }
    ]
    var col = [];
    for (var i = 0; i < bills.length; i++) {
        for (var key in bills[i]) {
            if (col.indexOf(key) === -1) {
                col.push(key);
            }
        }
    }

    // Create a table.
    var table = document.createElement("table");

    // Create table header row using the extracted headers above.
    var tr = table.insertRow(-1);                   // table row.

    for (var i = 0; i < col.length; i++) {
        var th = document.createElement("th");      // table header.
        th.innerHTML = col[i];
        tr.appendChild(th);
    }
    // add json data to the table as rows.
    for (var i = 0; i < bills.length; i++) {

        tr = table.insertRow(-1);

        for (var j = 0; j < col.length; j++) {
            var tabCell = tr.insertCell(-1);
            tabCell.innerHTML = bills[i][col[j]];
        }
    }

    // Now, add the newly created table with json data, to a container.
    var divShowData = document.getElementById('showData');
    divShowData.innerHTML = "";
    divShowData.appendChild(table);



Vinayak - Worked as a Java Developer in industry for 2 years with hands on experience on various devops tools including
 Jenkins, Gradle, Maven, Version Control (Git, Subversion) and worked on various frameworks including Spring rest, Hibernate,
 and worked on Oracle, Sybase, DB2 RDBMS.
 Willing and exited to learn other Spring frameworks and Javascript frameworks like React, Vue.js.